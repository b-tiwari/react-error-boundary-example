# EXAMPLE PROJECT ERROR BOUNDARY IN REACT 16

# HOW TO RUN

- Clone this repository
- cd to the cloned repository folder on your terminal
- run the command `yarn install`
- run the command `yarn run dev`
